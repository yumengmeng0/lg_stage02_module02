<?xml version="1.0" encoding="utf-8" ?>
<user>

</user>

<!--
    xml注释
    1.XML必须进行文档声明
    version 版本
    encoding 编码

    2.XML文档声明必须在第一行

    3.XML中元素标签的命名规则
        3.1 标签定义不能使用空格或者冒号
        3.2 XML标签区分大小写

    4.XML有且只有一个根元素

    5.元素体可以是文本或者还是一个标签

    6.属性是元素的一部分，只能出现在元素的开始标签中
        6.1 属性值必须使用单引号或者双引号包裹
        6.2 一个元素标签可以定义多个属性
-->